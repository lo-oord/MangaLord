# تقرير مراجعة وإصلاح MangaLord

## نطاق المراجعة

تمت مراجعة مسارات جلب الأنمي والمانجا، البحث، المصادر، التفاصيل، الصور والأغلفة، المفضلة والتاريخ، الإعدادات، التحديثات، التنزيل والقراءة المحلية، المشغل، كاش الصور، وأتمتة البناء في GitHub Actions. تمت المراجعة أولًا بقراءة الـ Architecture والكود الحالي، ثم أُجريت إصلاحات محددة دون إعادة تصميم التطبيق أو حذف ميزات قائمة.

## المشاكل التي تم العثور عليها وإصلاحها

| المستوى | النظام | المشكلة | الإصلاح |
|---|---|---|---|
| High | مصادر المانجا | بعض مصادر HTML كانت تستخرج الصورة من البطاقة نفسها بدل عنصر `img` الداخلي، فتظهر النتائج بلا غلاف. | أصبح parser يبحث عن `img` و`picture source` داخل البطاقة قبل قراءة خصائص الصورة. |
| High | مصادر المانجا | عقود `searchUri` لم تكن مكتملة لجميع تطبيقات `HtmlMangaSource`، ما كان يمنع التحليل البرمجي. | أضيفت مسارات البحث المطلوبة لمصادر Manga Swat وHijala. |
| High | المصادر الإضافية | تغيّرت بنية مصادر Manga Swat وHijala في الفرع أثناء المراجعة، وتم دمج النسخة الأحدث من GitHub بدل الكتابة فوقها. | تم حل تعارض الدمج بالحفاظ على adapters الأحدث ثم إعادة تطبيق إصلاح الصور. |
| High | البحث | نتائج بحث قديمة كانت تستطيع الكتابة فوق نتائج بحث أحدث بعد اكتمال طلب الشبكة. | أضيف request generation guard لبحث المانجا والأنمي. |
| High | التحديثات | فحص الإصدار كان يعتمد على `name` بدل `tag_name`، ويقارن النصوص بدل SemVer، مع Regex غير دقيق. | أصبح يستخدم `tag_name`، يطبع بادئة `v`، ويتحقق من مقارنة SemVer صحيحة. |
| High | التحديثات والإشعارات | كان الفحص الدوري يقتصر على المفضلة ويتوقف عند تعطيل الإشعارات. | أصبح تحديث الفصول يشمل المانجا المحفوظة كل ساعة، مع جعل الإشعارات اختيارية فقط. |
| High | المفضلة | إزالة المفضلة لم تكن تستدعي `setState`، لذلك قد تبقى الواجهة تعرض العنصر بعد الإزالة. | أصبحت إضافة/إزالة المفضلة تحدث داخل `setState` وتزامن التخزين والسحابة. |
| High | المصادر المحفوظة | فتح عنصر محفوظ بعد تعطيل مصدره كان يستخدم المصادر المفعّلة فقط، وقد يؤدي إلى مصدر خاطئ أو `StateError`. | أصبح حل المصدر يستخدم registry الكامل مع fallback آمن، مع تنظيف مفاتيح المصادر القديمة. |
| Medium | التصفح | شاشة الأنمي كانت تدعم البحث فقط رغم وجود adapters لجلب أحدث الأنميات. | أصبحت تحمل latest عند فتح الشاشة وعند ترك حقل البحث فارغًا. |
| Medium | التنزيل | مسار كتابة تنزيلات Azora كان مختلفًا عن مسار القراءة والحذف، كما لم تكن هوية المصدر ضمن المسار. | أصبح هناك مسار canonical واحد يتضمن `sourceKey` ويُستخدم في الكتابة والاستعادة والقراءة والحذف. |
| Medium | التنزيل | إمكانية تشغيل `resume` مرتين لنفس الفصل كانت تسبب مهام متنافسة. | أصبح `resume` يرفض بدء مهمة إذا كانت المهمة نفسها قيد التشغيل. |
| Medium | التنزيل | طلب صورة عالق كان يترك التنزيل في حالة غير نهائية. | أضيف timeout مدته 45 ثانية لكل طلب صورة. |
| Medium | كاش الصور | مفتاح الكاش كان يستخدم `path` فقط، ما يسبب تصادمًا بين مضيفين أو query strings مختلفة. | أصبح مفتاح الكاش يعتمد على URL الكامل دون fragment. |

## ما تم تعديله

- `lib/services/additional_manga_sources.dart`
- `lib/screens/app_screen.dart`
- `lib/services/download_manager.dart`
- `lib/configs/versions.dart`
- `lib/screens/components/images.dart`
- `lib/screens/anime_screen.dart`
- `REVIEW_REPORT.md`

## التحقق النهائي

تم تنفيذ التحقق عبر GitHub Actions فقط، دون بناء محلي:

- Dart formatting: ناجح.
- فحص مراجع المصادر القديمة: ناجح.
- `flutter analyze`: ناجح.
- اختبارات الوحدة والـ widget: ناجحة.
- اختبارات adapters الخاصة بمصادر الأنمي: ناجحة.
- بناء Android release APK: ناجح.
- التحقق من وجود APK: ناجح.
- رفع APK وcoverage وobfuscation symbols: ناجح.

رابط التشغيل الناجح: [GitHub Actions run 34261761601](https://github.com/lo-oord/MangaLord/actions/runs/34261761601)

آخر commit خاص بإكمال عقود المصادر: `211b9e4`، والـ commit المبني بنجاح هو `211b9e4`.

## مشاكل متبقية لم تُصلح مباشرة

- توقيع Android ما زال يعتمد على debug signing في إعداد المشروع الحالي؛ تحويله إلى release signing يحتاج keystore وGitHub Secrets من مالك المشروع، لذلك لم يتم اختلاق مفاتيح أو تغيير إعدادات الأمان.
- الترجمة الكاملة للعربية والإنجليزية ما زالت تحتاج طبقة `AppLocalizations` وملفات ARB؛ الموجود حاليًا يغيّر Locale لكنه لا يغطي كل النصوص الثابتة.
- حفظ موضع الصفحة داخل قارئ المانجا غير مكتمل، كما أن بعض مسارات القارئ لا تمر عبر كاش موحد؛ يحتاج ذلك قرارًا واضحًا حول نموذج التقدم المطلوب.
- رفع صورة الحساب في Appwrite يحتاج تهيئة Storage bucket وصلاحياته خارج الكود.
- اختبار التكامل الأصلي يحتاج جهاز/محاكي Android مناسبًا، بينما Workflow الحالي تحقق من التحليل والاختبارات وبناء APK بنجاح.
- تحذيرات deprecated الخاصة بإصدارات بعض GitHub Actions لا تمنع البناء، لكنها تحتاج ترقية دورية إلى الإصدارات الأحدث.

## إعدادات خارج الكود

لإخراج نسخة إنتاجية قابلة للنشر، يجب إضافة keystore آمن وSecrets الخاصة بالتوقيع إلى GitHub، ثم ربطها في `android/app/build.gradle`. كما يجب تهيئة Appwrite Storage إذا كانت ميزة صورة الحساب مطلوبة، وإضافة ARB/delegates إذا كان دعم الترجمة الكامل مطلوبًا.
