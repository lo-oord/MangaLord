# نتائج فحص المصادر

## Animerco
الموقع الحالي متاح على `https://det.animerco.org/animes/`. القائمة تحتوي روابط الأعمال بصيغة `/animes/{slug}/`، وتظهر صور الأغلفة داخل بطاقات الأعمال. يلزم فحص صفحة التفاصيل والحلقة لاستخراج selectors التشغيل الفعلية.

## AnimeLek
النطاق القديم `animelek.cc` يعيد التوجيه حالياً إلى `animedar.net/anime-p/`. بنية القائمة الجديدة تستخدم مسارات `/anime-p/{slug}/`، وصفحة التفاصيل تعرض الجودة FHD/HD/SD وقائمة سيرفرات متعددة مثل SEGAVID وDOOD وOK وVIDBEM وUPTOSTREAM وMP4UPLOAD وMEGA. parser الحالي على `animelek.cc/anime/` غير متوافق مع النطاق والبنية الحالية.

## Animerco — صفحة التفاصيل
صفحة التفاصيل تستخدم مسارات الأعمال `/animes/{slug}/`، لكن قائمة المواسم تحتوي روابط منفصلة بصيغة `/seasons/{slug}-season-1/`. صفحة العمل تعرض الموسم وعدد الحلقات، بينما روابط الحلقات لا تظهر في صفحة العمل نفسها؛ لذلك يجب أن يقرأ parser صفحة الموسم ثم يستخرج روابط الحلقات منها. صفحة التفاصيل تستخدم HTML تقليدياً وليست API واضحة.

## Animerco — الموسم والحلقات
صفحة الموسم تعمل على `/seasons/{slug}-season-1/` وتحتوي روابط الحلقات على `/episodes/{slug}-الحلقة-1/` ونحوها. صفحة الموسم تعرض قائمة الحلقات فعلياً، كما تعرض معلومات العمل والوصف والتصنيفات. يجب أن يقرأ parser روابط `a[href*="/episodes/"]` من صفحة الموسم أو من صفحة العمل، ثم يستخدم صفحة الحلقة لاستخراج iframe/video.

## Animerco — صفحة الحلقة
صفحة الحلقة تستخدم `/episodes/.../` وتعرض خوادم متعددة عبر عناصر `a` ذات المعرّفات `option-1` إلى `option-12`، وتحتوي رابط تشغيل تفاعلي `#click-player`. لا تظهر روابط mp4/m3u8 مباشرة في النص الأولي؛ لذلك يجب استخراج href/data attributes لخوادم الخيارات أو تنفيذ endpoint/طلب الصفحة الذي يكشف رابط iframe قبل تمرير المصدر إلى مشغل التطبيق. روابط الحلقات نفسها موجودة في نفس صفحة الحلقة.
