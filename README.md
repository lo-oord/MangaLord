# MangaLord

MangaLord is a cross-platform manga client built with Flutter and Rust. It supports Android, iOS, macOS, Windows, and Linux.

## Building

Builds are performed by GitHub Actions. Push to `main` or start the **Build MangaLord** workflow manually from the **Actions** tab. The workflow uploads the generated Android APK as an artifact; no local build is required.
